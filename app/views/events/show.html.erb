<div class="col-sm-9 sty-background-default sty-margin-box">
    <div class="row gutter">
        <div class="col-lg-12 col-md-12">
            <h1 class="page-title3"><%= @event.title %></h1>
            <% if @event.image? %>
            <figure style="text-align: center;margin-bottom: 30px;">
                <br><img src="<%= @event.image.url %>" alt="<%= @event.title %>" class="img-news">
            </figure>
            <span class="news-meta-date"><b>Data: </b><%= @event.date.strftime("%d/%m/%Y")%> - <%= @event.date.strftime("%H:%M")%> <br/> <b>Local: </b> <p> <%= @event.place %></p></span>
            <div class="news-body2">
                <p class="call-out">
                    <p align="justify">
                        <%= @event.description.html_safe %>
                    </p>
                </p>
            </div>
            <% else %>
            <span class="news-meta-date"><b>Data: </b><%= @event.date.strftime("%d/%m/%Y")%> - <%= @event.date.strftime("%H:%M")%> <br/> <b>Local: </b> <p> <%= @event.place %></p></span>
            <div class="news-body2">
                <p class="call-out">
                    <p align="justify">
                        <%= @event.description.html_safe %>
                    </p>
                </p>
            </div>
            <% end %>
        </div>
    </div>
    <div class="clear"></div>
 </div>
